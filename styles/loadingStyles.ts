import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#9DDD84",
  },

  title: {
    fontSize: 28,
    fontWeight: "bold",
    color: "#2C0660",
    textAlign: "center",
  },
});
