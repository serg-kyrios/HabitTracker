import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    paddingVertical: 12,
    paddingHorizontal: 20,
    borderRadius: 12,
    alignItems: "center",
    marginVertical: 6,
  },

  text: {
    color: "#fff",
    fontWeight: "600",
    fontSize: 16,
  },

  secondary: {
    backgroundColor: "#eee",
  },

  textSecondary: {
    color: "#333",
  },

  pressed: {
    opacity: 0.8,
  },
});
